import FeedbackCard from '@/domains/feedback/ui/FeedbackCard';

export default function FeedbackListPage() {
    return (
        <div className="flex flex-col items-center py-[20px] gap-[8px] w-full">
            <FeedbackCard />
        </div>
    );
}
